const numbers = [34, 109, 18, 56, 1, 4, 78, 99]

// filter
const filteredNumbers = numbers.filter((number) => number > 10)
console.log(filteredNumbers)

// map
const mappedNumbers = numbers.map((number) => number * 2)
console.log(mappedNumbers)
